# 外部资料边界

RH124、RH134、RH294、模拟题、原始课件和截图不进入 Git。它们由维护者在仓库外保存，只作为内容复核材料；Markdown/YAML 不依赖这些大文件才能构建。

需要在本地工具中引用资料时，可设置：

```bash
export RHEL_SOURCE_ROOT=/path/to/redhat-materials
```

不得把该目录复制到 `sources/` 或提交到仓库。
